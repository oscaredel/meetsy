<% if session[:uuid] %>

  <%= simple_form_for [@event, @update] do |u| %>
    <%= u.input :text, label: false, placeholder: "Say something" %>
    <%= u.submit "Send", class:"btn btn-success" %>
  <% end %>

<% end %>
