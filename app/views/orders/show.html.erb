<div class="container">
  <div class="row">
    <div class="col">
      <h1>Upgrade order <small><span class="badge badge-primary"><%= @order.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ORDER ID</strong> <%= @order.id %></li>
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>EVENT</strong> <%= @order.event.name %></li>
        <li><strong>TOTAL</strong> <%= @order.amount %></li>
        </li>
      </ul>
      <p>Enjoy!</p>
    </div>
  </div>
</div>
