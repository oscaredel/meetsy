<div class="row">
  <div class="col p-3 m-3 bg-dark rounded pane text-center shadow">

    <h5 class="pane-name">Snapbox</h5>
    <img src="<%= cl_image_path(@photo.image.key, crop: :fill) %>">
    <p class="">📸 <%= @photo.contact.name %></p>
    <% if @photo.contact.id == session[:uuid] %>
      <%= link_to icon('fas', 'trash'), event_photo_path(@event, @photo), method: :delete, data: { confirm: "Are you sure you want to remove this photo?" }, class:'text-danger d-inline' %>
    <% end %>
    <br>
    <br>
    <%= link_to "back to all photos", event_photos_path(@event), class:'btn btn-light' %>
  </div>
</div>
